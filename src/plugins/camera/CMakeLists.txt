cmake_minimum_required(VERSION 3.14)

if(UNIX AND NOT APPLE)
    add_subdirectory(V4L2Camera)
    if(ENABLE_CAMERA_HIKVISION)
        add_subdirectory(HikvisionCamera)
    endif()
elseif(WIN32)
    add_subdirectory(DirectShowCamera)
endif()
